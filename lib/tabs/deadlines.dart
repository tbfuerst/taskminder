import 'package:flutter/material.dart';

class DeadlinesTab extends StatelessWidget {
  const DeadlinesTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("deadlines");
  }
}
